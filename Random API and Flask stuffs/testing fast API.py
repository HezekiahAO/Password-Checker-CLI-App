dict = {
    0:"Daniel",
    1:name = "Emmanuel",   # Each value is an instance of the Item class, meaning each key-value pair represents an item with attributes defined in the class.
}
print(dict
      )